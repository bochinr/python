b = []
a = ("红楼梦", ["贾小玉"], "55元", "XX出版社", "2022年2月22日", "小树林")
b = a[1]
# 修改元组
b.append("林黛玉")  # 增加人物名 林黛玉
print(a)

b[0] = "贾宝玉"  # 修改贾小玉为贾宝玉
print(a)

# 修改元组的值，可先将元组转换为列表类型，然后再转变为元组类型
c = list(a)
del a  # 使用del删除原来的元组a
for i in range(len(c)):
    if c[i] == "55元":
        c[i] = "50元"
if c[i] == "小树林":
    c.remove("小树林")
a = tuple(c)  # 重新转换为元组类型
print(a, type(a))  # 打印a元组，并显示其数据类型
